spring:
  profiles:
    active: production
logging:
  level:
    root: ${LOGGING_LEVEL:WARN}

line-bot:
  channels:
    tdl:
      token: "Please set value in production yaml."
    ryobot:
      token: "Please set value in production yaml."
